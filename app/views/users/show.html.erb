<div class="user-show-c">
    <div class="user-box">
        <img class="user-img" src=<%= @user.Photo %> alt="user"/>
        <div class="user-info-box">
            <h2 class="user-name"><%= @user.Name %></h2>
            <p class="user-post-count">Number of posts: <%= @user.PostsCounter%></p>
        </div>
    </div>

    <p class="user-bio"><%= @user.Bio %></p>


    <% @user.three_recent_posts.each do |post| %>
        <%= link_to user_post_path(@user.id, post.id), class:"text-dec" do %> 
        <div class="user-recent-post">
            <h2 class="user-post-title">Post : <%= post.Title %></h2>
            <p class="user-post-text"><%= post.Text %></p>
            <h3 class="user-post-counters">Comments: <%= post.CommentsCounter%>, Likes: <%= post.LikesCounter %><h3>
        </div>
        <% end %>
    <% end%>

    <%= render partial: "posts/delete_post", local: { post: @post } %>

    <button class="button">
        <%= link_to "See all posts", user_posts_path(@user.id), class:"text-dec" %>
    </button>
</div>